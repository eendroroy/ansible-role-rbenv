---
- name: Check for previous installation of ruby-build
  stat:
    path: '{{ ruby_build.path }}'
  ignore_errors: true
  register: ruby_build_dir

- name: Clone ruby-build
  git:
    repo: '{{ ruby_build.url }}'
    dest: '{{ ruby_build.path }}'

  when: ruby_build_dir.stat.exists == false